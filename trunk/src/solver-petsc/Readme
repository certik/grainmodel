Install PETSC and SLEPC, then edit Makefile and change

PETSC_DIR=/home/ondra/slepc/petsc-2.3.1-p11
SLEPC_DIR=/home/ondra/slepc/slepc-2.3.1

to point to your PETSC and SLEPC installation.

Compile using:

make

Run either manually, or using the script "run".
